#!/bin/sh

# Echos green color if bluetooth is powered or red color otherwise

bstatus="$(bluetoothctl show | grep Powered | sed -n -e 's/^\tPowered: \(.*\)/\1/p')"
echo 
echo 
[ $bstatus = "yes" ] && echo '#00ff00' || echo '#ff0000'
[ $BLOCK_BUTTON ] && $TERMINAL -e bluetoothctl >/dev/null 2>&1
