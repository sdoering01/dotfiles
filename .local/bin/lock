#!/bin/sh

# Locks screen and pauses dunst notifications until screen is unlocked
# Passes its parameters to i3lock

prev_state=$(dunstctl is-paused)
dunstctl set-paused true
i3lock -n $@
dunstctl set-paused $prev_state
